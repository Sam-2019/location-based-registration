import React from "react";
import { Wrapper, Load } from "../styledComponents";

export default function Loader() {
  return (
    <Wrapper>
      <Load>Loading....</Load>
    </Wrapper>
  );
}
