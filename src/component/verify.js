import React from "react";
import "./form.css";

export default function Verify() {
  const verify = () => {
    //const userID = localStorage.getItem("username");
    //check db for userID
    //get userName and department from users dB
    //populate form with userData (readOnly)
    //hit submit to register
    //send success message if data hits dB
  };

  return (
    <>
      <input type="submit" />
    </>
  );
}
