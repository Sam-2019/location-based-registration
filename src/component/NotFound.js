import React from "react";
import { Wrapper, Load } from "../styledComponents";

function NotFound() {
  return (
    <Wrapper>
      <div>No Page</div>
    </Wrapper>
  );
}

export default NotFound;
