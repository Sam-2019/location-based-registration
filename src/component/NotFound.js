import React from "react";
import { Wrapper, Load } from "../styledComponents";

function NotFound() {
  return (
    <Wrapper>
      <Load>
        <div>No Page</div>
      </Load>
    </Wrapper>
  );
}

export default NotFound;
